#!/usr/bin/env python3
"Type anotated floor function"
import math


def floor(n: float) -> int:
    """
    Takes a float number, rounds it to the floor and returns an int
    floor(6.16) = 6
    """
    return math.floor(n)
